# keryx-smtp-gateway

Keryx SMTP Gateway is a service that provides an SMTP interface for sending email through AWS SES.

## Overview

- Acts as an SMTP wrapper over AWS SES API.
- Intended to simplify integration for applications that already support SMTP.